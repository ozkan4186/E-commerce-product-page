const menu= document.querySelector(".hamb")
menu.addEventListener("click",(e)=>{
    if(e.target.claslist.contains("hambclick")){
        e.target.claslist.remove("hambclick")
    }
})
const artı=document.querySelector()